from setuptools import setup, find_packages

setup(
    name='srunner',
    version='0.1.0',
    url='https://github.com/carla-simulator/scenario_runner',
    author='CARLA Team',
    author_email='',
    description='Not an official release',
    packages=find_packages(),
    install_requires=[],
)
